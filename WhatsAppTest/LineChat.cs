using System;
using Com.CloudRail.SI;
using Com.CloudRail.SI.Services;
using Com.CloudRail.SI.Types;
using TLSharp.Core;

namespace WhatsAppTest
{
    public class LineChat
    {

        public void SendLineMessage(string receiverID, string message)
        {
            /*
            CloudRail.AppKey = "";

            Line service = new Line(null, "");

            Message newMessage = service.SendMessage(receiverID, message);*/
        }
    }
}